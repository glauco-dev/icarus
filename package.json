{
  "name": "icarus",
  "description": "The default Marko starter app",
  "version": "1.0.0",
  "type": "module",
  "dependencies": {
    "marko": "^5.31.17"
  },
  "devDependencies": {
    "@marko/run": "^0.3.0",
    "sass": "^1.69.5"
  },
  "private": true,
  "scripts": {
    "dev": "marko-run",
    "build": "marko-run build && cp -r ./src/api ./dist/",
    "preview": "marko-run preview",
    "start": "node --enable-source-maps ./dist/index.mjs"
  }
}
