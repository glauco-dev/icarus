<!-- $ const PocketBase = require('pocketbase');

$ console.log(PocketBase) -->
import PocketBase from 'pocketbase';

$ const pb = new PocketBase('http://127.0.0.1:8090');

$ pb.collection('teste').subscribe('*', function (e) {
    console.log("Yey",e.record);
});
